{
  "name": "scripts",
  "version": "1.0.0",
  "description": "This folder includes `storage_e2e_test.mjs`, a Node script that validates Firebase Storage rules and filename conventions by:\r - Creating a minimal Firestore `videos/{videoId}` document\r - Uploading `thumbnails/{videoId}.jpg`\r - Fetching a signed URL and printing `Cache-Control`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "node-fetch": "^3.3.2"
  }
}
